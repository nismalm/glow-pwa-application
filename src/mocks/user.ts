import type { UserProfile } from '@/types/models'

// Phase 6: replace with real user from Firebase Auth
export const mockUser: UserProfile = {
  displayName: 'Dilsath',
  avatarInitial: 'D',
  waterGoal: 10,
}
